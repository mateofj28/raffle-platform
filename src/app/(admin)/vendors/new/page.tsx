import { PageHeader } from "@/components/shared/page-header";

export default function NewVendorPage() {
    return (
        <div>
            <PageHeader title="Nuevo Vendedor" description="Registra un nuevo vendedor" />
            <p className="text-default-500">Formulario de vendedor (pendiente)</p>
        </div>
    );
}
