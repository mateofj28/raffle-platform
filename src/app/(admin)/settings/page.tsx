import { PageHeader } from "@/components/shared/page-header";

export default function SettingsPage() {
    return (
        <div>
            <PageHeader title="Configuración" description="Ajustes de la plataforma" />
            <p className="text-default-500">Configuración del tenant (pendiente)</p>
        </div>
    );
}
