import { PageHeader } from "@/components/shared/page-header";

export default function NewCustomerPage() {
    return (
        <div>
            <PageHeader title="Nuevo Cliente" />
            <p className="text-default-500">Formulario de cliente (pendiente)</p>
        </div>
    );
}
